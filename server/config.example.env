# Emdash Server Environment Example
# ----------------------------------------------------------------------
# Copy this file to .env (or export the variables another way) before
# starting cmd/emdash-server. Never commit real secrets to git.

###############################################################################
# Development defaults
###############################################################################
# Shared secret used to sign HS256 JWTs. Replace before exposing the server.
AUTH_SECRET=dev-secret-change-in-production

# Toggle TLS/WSS protection for gRPC (:50051) and WebSocket (:8080) listeners.
TLS_ENABLED=false

# Certificate/key paths when TLS is enabled. The defaults match scripts/gen-cert.sh
# output and are relative to the server/ directory.
TLS_CERT_FILE=certs/server.crt
TLS_KEY_FILE=certs/server.key

###############################################################################
# Production template (uncomment + edit)
###############################################################################
# AUTH_SECRET=use-a-random-32-byte-secret-generated-by-your-secret-manager
# TLS_ENABLED=true
# TLS_CERT_FILE=/etc/letsencrypt/live/api.example.com/fullchain.pem
# TLS_KEY_FILE=/etc/letsencrypt/live/api.example.com/privkey.pem

# SECURITY NOTES:
# - Rotate AUTH_SECRET regularly; all clients must refresh their JWTs afterward.
# - Restrict file permissions on TLS_CERT_FILE/TLS_KEY_FILE to the service user.
# - Store this file outside of source control for production deployments.
